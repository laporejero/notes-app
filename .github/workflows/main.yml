name: Notes app CI

on:
    push:
        branches:
            - main
jobs:
    hello:
        runs-on: ubuntu-latest

        steps:
            - name: Say hello
              run: echo "Github Actions is working!"